namespace bestBuild.DAL.Data.Enums;

public enum Brands
{
    AKS_LIGHT, AKS, GTV, Blum
};
public enum Fastening
{
    SCREW, EURO_SCREW
};

public enum TypeHinge
{
    OVERHEAD, SEMI_OVERHEAD, INNER
};

public enum MaterialTypeHinge
{
    DSP, LDSP, MDF, Massiv
}
